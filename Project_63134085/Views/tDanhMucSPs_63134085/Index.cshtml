@model PagedList.IPagedList<Project_63134085.Models.tDanhMucSP>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutOgani.cshtml";
}

<style type="text/css">
    .featured__item__text {
        text-transform: capitalize;
    }

    .line-through {
        text-decoration: line-through;
    }
</style>
<div id="displayProducts" class="row featured__filter">
    @foreach (var item in Model)
    {
        <div class="col-lg-3 col-md-4 col-sm-6 mix oranges fresh-meat">
            <div class="featured__item">
                <div class="featured__item__pic set-bg" data-setbg="@Url.Content("../Images/"+item.AnhDaiDien)" style="background-image: url(&quot;img/featured/feature-1.jpg&quot;);">                    
                </div>
                <div class="featured__item__text">
                    <h6><a href="@Url.Action("Details", new { maSp = item.MaSP })">@item.TenSP</a></h6>
                    <span class="line-through">@string.Format("{0:#,0}", item.GiaLonNhat)đ</span>
                    <h5>@string.Format("{0:#,0}", item.GiaNhoNhat)đ</h5>
                </div>
            </div>
        </div>
    }
</div>
<br />
<div class="product__pagination">
    @Html.PagedListPager(Model, page => Url.Action("Index", new { page = page }))
</div>